## Not necessary for Linux or Mac, but fails installation if not present for Windows
useDynLib(CDS)
exportPattern("^[[:alpha:]]+")

import(methods, Rcpp, RCurl, XML, testthat)

exportClasses(CDS)
exportMethods("summary",
              "show")

