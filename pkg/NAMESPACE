if (.Platform$OS.type == "windows")
    useDynLib(CDS)

exportPattern("^[[:alpha:]]+")

import(methods, Rcpp, RCurl, XML, testthat)

exportClasses(CDS)
exportMethods("summary",
              "show")

