% Generated by roxygen2 (4.0.1): do not edit by hand
\name{get.date}
\alias{get.date}
\title{\code{get.date} returns appropriate dates used in pricing a CDS contract.}
\usage{
get.date(date, maturity = NULL, tenor = NULL)
}
\arguments{
\item{date}{the date for which we are calculating the relevant dates. Must be of class "date".}

\item{maturity}{date of maturity of the CDS contract. Default is NULL. Must be of class "date"}

\item{tenor}{numeric tenor of the CDS contract in format like 5Y. Default
is NULL. Must enter either maturity or tenor, but not both.}
}
\value{
a date frame with 8 variables: step-in date (T+1), value date (T+3 business days),
  start date (accrual begin date), end date (maturity), backstop date (T-60
  day look back from which 'protection' is effective), pen coupon date
  (second to last coupon date)
}
\description{
\code{get.date} returns appropriate dates used in pricing a CDS contract.
}
\examples{
get.date(as.Date("2014-05-07"), tenor = 5, maturity = NULL)
}

